// Define an arrow function named 'c' that takes two parameters, num1 and num2
let c = (num1, num2) => {
    // Return the sum of num1 and num2
    return (num1 + num2);
}

// Call the function 'c' with arguments 2 and 1, and log the result to the console
console.log(c(2, 1));  // Output will be 3