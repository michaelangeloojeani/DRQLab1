// Create an arrow function that takes an argument and returns it
let a = (value) => {
    return value;
  };
  
  //Call the function with a value and output it to the console
  console.log(a("Data Representation & Querying"));
  